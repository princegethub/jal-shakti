import express from 'express';

/**
 * Creates an instance of an Express router to handle admin-related routes.
 */
const authRoutes = express.Router();
// authRoutes.post('/login', validate(loginAdminSchema), login);
// authRoutes.delete('/logout', logout);
// authRoutes.post('/refresh', refreshToken);
// authRoutes.get('/login-token', authenticateUser, getLoginToken);
// authRoutes.post('/verification', verifyOtp);
export default authRoutes;
